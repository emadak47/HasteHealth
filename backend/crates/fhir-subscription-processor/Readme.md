# FHIR Subscription Processor

In memory filter for given set of Resources and subscription.

Notes Subscriptions `triggers` are an abstraction so that they can be derived from both a `R4 Subscription resource` and `SubscriptionTopic` backport.
